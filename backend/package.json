{
  "name": "backoffice-backend",
  "version": "1.0.0",
  "description": "Backend API for backoffice system",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "db:setup": "mysql -u root -p < mysql-setup.sql",
    "db:test": "node -e \"require('./src/utils/mysql').initDatabase().then(() => console.log('Database connection successful')).catch(e => console.error('Database connection failed:', e))\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^9.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
